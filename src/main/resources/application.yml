spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true     # ✅ 이게 있어야 콘솔 접속 가능
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
